|  # | Package Normally Used                              | Category                            | Standard Library Replacement                                                   | Rationale                                                                                                                   |
| -: | -------------------------------------------------- | ----------------------------------- | ------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------- |
|  1 | `PyTorch` / `TensorFlow` / `LibTorch`              | Deep Learning Engine & Autograd     | Handcrafted 1D/3D tensors, analytical backpropagation, matrix math             | Replaces large ML frameworks with pure Go analytical gradients and zero third-party dependency risk.                        |
|  2 | `pandas` / `polars`                                | Tabular DataFrames & Profiling      | Custom DataFrame using `encoding/csv`, `strconv`, `math`, `sort`               | Provides numeric/categorical column profiling, missing-value counts, min/max/mean/std, and data previews.                   |
|  3 | `scikit-learn` (`StandardScaler`, `OneHotEncoder`) | Feature Engineering & Preprocessing | Custom data transformation with mean/std and min/max scaling, one-hot encoding | Encodes CSV datasets into normalized float tensors and inverse-transforms predictions with metadata persistence.            |
|  4 | `scikit-learn` (`metrics`)                         | Model Evaluation Metrics            | Custom classification and regression metric calculations                       | Calculates confusion matrix, precision, recall, Macro-F1, MSE, RMSE, MAE, and R² natively.                                  |
|  5 | `scikit-learn` (`datasets`)                        | Benchmark Datasets                  | Embedded string constants and synthetic mathematical manifold generators       | Provides Iris, Wine, Cancer, Titanic, Housing, Moons, Spirals, XOR, and Sine datasets without disk or network dependencies. |
|  6 | `torchvision.datasets.ImageFolder`                 | Vision Dataset Loader               | Directory scanning using `os.ReadDir`, `path/filepath`, and `sort`             | Dynamically scans arbitrary folder structures containing any number of class subfolders.                                    |
|  7 | `OpenCV` (`cv2`) / `Pillow`                        | Computer Vision & Image Processing  | `image`, `image/color`, `image/draw`, `image/png`, `image/jpeg`                | Provides bilinear interpolation, bounding-box cropping, margin centering, and grayscale conversion.                         |
|  8 | `torch.optim.Adam` / `SGD`                         | Optimization Algorithms             | Standard Go structs with moment tracking, `math.Sqrt`, and `math.Pow`          | Implements Adam with bias correction and SGD with momentum and L2 weight decay.                                             |
|  9 | `CUDA` / `OpenMP`                                  | Multithreading & Parallelism        | `sync.WaitGroup`, Go goroutines, and `runtime.NumCPU()`                        | Parallelizes computationally intensive operations across available CPU cores.                                               |
| 10 | `Flask` / `FastAPI` / `Express`                    | Web Backend & API                   | `net/http` and `encoding/json`                                                 | Provides HTTP serving, static SPA delivery, and REST API handlers using the Go standard library.                            |
| 11 | `Chart.js` / `D3.js` / `Plotly`                    | Live Training Curves & Charts       | Inline dynamic SVG paths and native HTML5 Canvas                               | Renders live loss and accuracy curves and confusion-matrix visualizations without external JavaScript libraries.            |
| 12 | `ONNX` / `Pickle` / `SafeTensors`                  | Model Serialization                 | `encoding/binary`, `encoding/json`, `os.Create`, and `os.Open`                 | Implements binary weight serialization with JSON model-pipeline metadata.                                                   |
| 13 | `Albumentations`                                   | Data Augmentation                   | Native matrix pixel transforms, 2D rotations, and shifts                       | Expands datasets using pure Go pixel-coordinate transformations.                                                            |
